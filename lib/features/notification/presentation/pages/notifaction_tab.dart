import 'package:flutter/material.dart';

class NotifactionTab extends StatelessWidget {
  const NotifactionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}